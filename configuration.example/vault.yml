---
# Host secrets — fill in your values, then run `make encrypt`

# Host connection
CHANGEME_HOST_NAME_ip: "CHANGEME"

# Discord integration
CHANGEME_HOST_NAME_bot_token: "CHANGEME"
CHANGEME_HOST_NAME_discord_allowed_users: "CHANGEME"
CHANGEME_HOST_NAME_discord_home_channel: "CHANGEME"

# Honcho memory backend
CHANGEME_HOST_NAME_honcho_base_url: "CHANGEME"
CHANGEME_HOST_NAME_honcho_api_key: "CHANGEME"
CHANGEME_HOST_NAME_honcho_db_password: "CHANGEME"

# Shared defaults
default_firecrawl_api_key: "CHANGEME"
default_firecrawl_base_url: "CHANGEME"
default_git_user_name: "CHANGEME"
default_git_user_email: "CHANGEME"
